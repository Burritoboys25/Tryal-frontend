import Container from '@/shared/components/layout/Container'

const Waitlist = () => {
  return (
    <section className="-mt-[72px] flex min-h-screen items-center justify-center py-20">
      <Container className="max-w-md text-left">
        <h1 className="text-h2 mb-4">Get early access!</h1>
        <p className="text-left">
          We&apos;re building something exciting — the ultimate platform for discovering experiences
          and activities. Be the first to know when we launch and get exclusive early updates.
        </p>

        <form></form>
      </Container>
    </section>
  )
}

export default Waitlist
